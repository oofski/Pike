# Bundled Resources

Files in this directory are packaged into the app under `resources/` at runtime
(`process.resourcesPath`). The Mac iMessage bridge is bundled here so Rush Chairs
can extract and run it from the installed app.
